@model ZapWatch.Web.Models.Account.LoginViewModel
@{
    ViewData["Title"] = "Login";
}

<div class="zw-form-card mx-auto">
    <p class="zw-eyebrow mb-1 text-center">Welcome back</p>
    <h1 class="mb-4 text-center">Log in</h1>

    <div class="zw-card p-4">
        @if (TempData["ExternalLoginError"] is string externalError)
        {
            <div class="alert alert-warning">@externalError</div>
        }
        <form asp-action="Login" method="post">
            <input type="hidden" asp-for="ReturnUrl" />
            <div asp-validation-summary="All" class="text-danger mb-3"></div>
            <div class="form-group mb-3">
                <label asp-for="Email" class="form-label"></label>
                <input asp-for="Email" class="form-control" autocomplete="username" />
                <span asp-validation-for="Email" class="text-danger"></span>
            </div>
            <div class="form-group mb-3">
                <div class="d-flex justify-content-between align-items-center">
                    <label asp-for="Password" class="form-label mb-0"></label>
                    <a asp-controller="Account" asp-action="ForgotPassword" style="font-size:0.82rem;">Forgot password?</a>
                </div>
                <input asp-for="Password" class="form-control" autocomplete="current-password" />
                <span asp-validation-for="Password" class="text-danger"></span>
            </div>
            <div class="form-group form-check mb-4">
                <input asp-for="RememberMe" class="form-check-input" />
                <label asp-for="RememberMe" class="form-check-label"></label>
            </div>
            <button type="submit" class="btn btn-primary w-100">Log in</button>
        </form>
        @await Html.PartialAsync("_GoogleSignInButton", Model.ReturnUrl)
    </div>
    <p class="mt-3 text-center zw-muted">
        Don't have an account? <a asp-controller="Account" asp-action="Register">Sign up</a>
    </p>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
